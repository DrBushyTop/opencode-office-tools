import { excel } from "../lib/office-excel"

export default excel("get_workbook_info", "Get a lightweight workbook summary with worksheet names and the active sheet. Prefer get_workbook_overview for structural inspection.", {})
